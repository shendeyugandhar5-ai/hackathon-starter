-- ==============================================================================
-- Hackathon Starter - Database Initialization Script
-- Executed automatically when PostgreSQL Docker container is first initialized.
-- ==============================================================================

-- Enable UUID extension for unique identifier generation if needed tomorrow
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Enable pgcrypto for cryptographic functions if needed tomorrow
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- You can add problem-specific table seeds or initial lookups here tomorrow
