"""SQLAlchemy Models Package.

Tomorrow, define your problem-specific SQLAlchemy models in this directory.
For example:
    models/user.py -> class User(Base): ...
    models/item.py -> class Item(Base): ...

Then import them here so migrations and metadata can discover them:
    from app.database.base import Base
    from app.models.user import User
    from app.models.item import Item
"""
