# Backend API Base URL
VITE_API_BASE_URL=http://localhost:8000

# Demo student id used by every data hook. The backend seeds 'rahul' with
# mastery, prerequisite edges and recommendations so the dashboard isn't
# empty on first load. Set to empty to use real per-user ids instead.
VITE_DEMO_STUDENT_ID=rahul

# Supabase Authentication & Database Configuration
VITE_SUPABASE_URL=https://yulooaxeggshwmwgktcv.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_placeholder_replace_with_your_anon_key
